# ALSA configuration
# Routes audio to the default device. Adjust card/device numbers if needed.
# Run `aplay -l` to list available sound cards.

pcm.!default {
    type hw
    card 0
    device 0
}

ctl.!default {
    type hw
    card 0
}
